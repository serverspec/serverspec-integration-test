- name: centos65
  command: vagrant up centos65 --provider=digital_ocean && bundle exec itamae ssh  --no-color --host centos65 --vagrant recipe.rb && DIGITALOCEAN=true rake spec:centos65
  directory: $WORKING_DIR
